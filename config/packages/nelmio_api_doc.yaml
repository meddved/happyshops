nelmio_api_doc:
    documentation:
        info:
            title: HappyShops test api
            version: 0.0.1
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc

